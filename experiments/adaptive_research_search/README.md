# Adaptive Research Search

Does a plateau-aware branch controller recover a hidden generating mechanism faster than fixed or
naive-random search-diversity strategies, at matched compute, on a frozen local LLM?

This is a **separate research thread** from this repo's gateway/determinism-dividend north star
(`memory/ideas.md`). It shares no code with `gateway/` and does not change any existing
pre-registration or verdict.

Read `DESIGN.md` first (architecture, staged roadmap, what's reused vs. what's new), then
`PREREGISTRATION.md` (the frozen Stage 0 hypothesis, splits, controls, and decision gate).

## Status

2026-09-15: design + Stage 0 pre-registration written. No harness code yet — nothing in this
directory has run. Do not cite any number from here until `PREREGISTRATION.md`'s hidden-split
gate is read and a verdict lands in `memory/hypothesis-graveyard.md`.
