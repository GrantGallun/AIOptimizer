[
  {
    "id": "linkedin_scraper_ratelimit",
    "domain": "talenttrader",
    "prompts": [
      "We are building a LinkedIn talent-flow reader. Constraint for this whole task: every network call MUST go through a function named rate_limited_get and MUST NOT use requests.get directly. The output schema for a parsed profile is exactly: {name, headline, company, tenure_months}. No other keys.",
      "Start scraper.py with rate_limited_get(url) that sleeps 2 seconds between calls using time.sleep, and parse_profile(html) that returns the schema dict from the four fields.",
      "Now a separate concern: create notes.py with attach_note(profile, note) that appends a note string to a profile's '_notes' list, creating the list if absent.",
      "Add list_notes(profile) to notes.py returning the '_notes' list, or an empty list if the profile has none.",
      "Create models.py with a Candidate dataclass: name, email, company, years_experience.",
      "Add from_dict(data) and to_dict(candidate) helpers to models.py for Candidate.",
      "Create logging_setup.py with configure_logging(level='INFO') that installs a StreamHandler with a timestamp+level+message formatter and returns the root logger.",
      "Add an override to configure_logging so it reads the TALENTTRADER_LOG_LEVEL environment variable when set, taking precedence over the level argument.",
      "Create metrics.py with a Counter class: increment(key), get(key) defaulting to 0, and reset().",
      "Add a snapshot() method to Counter in metrics.py returning a plain dict copy of all current counts.",
      "Create cache.py with a memoize(fn) decorator that caches results by its positional args in a dict.",
      "Add cache_clear() to the object memoize returns, matching functools.lru_cache's convention.",
      "Create report.py with summarize_profiles(profiles) returning a dict of company -> count.",
      "Change summarize_profiles in report.py to return a list of (company, count) tuples sorted by count descending.",
      "Create storage.py with save_profiles_json(profiles, path) using json.dump with indent=2, creating parent directories as needed.",
      "Add load_profiles_json(path) to storage.py as the inverse of save_profiles_json, returning [] if the file does not exist.",
      "Create cli.py with a function read_url_list(path) that reads one URL per line, skipping blank lines and lines starting with '#'.",
      "Add build_parser() to cli.py with argparse accepting --urls (path) and --output (path, default 'profiles.json').",
      "Back in scraper.py: add a ScraperError(Exception) class for malformed input.",
      "Change parse_profile in scraper.py to raise ScraperError instead of letting a raw KeyError propagate when a required field is missing from html.",
      "Refine parse_profile in scraper.py so the company field is title-cased before being returned.",
      "Add a module-level docstring to scraper.py summarizing what it does, one sentence.",
      "In notes.py, make attach_note return the updated notes list instead of None, so callers can chain it.",
      "In models.py, add a __post_init__ validation to Candidate that raises ValueError if years_experience is negative.",
      "Now extend scraper.py: add fetch_profiles(urls) that retrieves and parses each URL in the list, returning a list of parsed profiles.",
      "Add a singular convenience wrapper fetch_profile(url) to scraper.py that calls fetch_profiles([url]) and returns the one result.",
      "Wire cli.py's main() to call read_url_list, then scraper.fetch_profiles on the result, then storage.save_profiles_json to --output, printing the count retrieved.",
      "Finalize scraper.py. Do not print anything at import time."
    ],
    "expected_files": ["scraper.py"],
    "requirements": [
      {"id": "uses_rate_limited_get", "must_include": ["rate_limited_get"], "is_constraint": true},
      {"id": "no_direct_requests_get", "must_exclude": ["requests.get("], "is_constraint": true},
      {"id": "schema_keys", "must_include": ["name", "headline", "company", "tenure_months"], "is_constraint": true},
      {"id": "sleeps", "must_include": ["time.sleep"]}
    ],
    "ast_checks": [
      {"kind": "defines", "name": "rate_limited_get"},
      {"kind": "defines", "name": "parse_profile"},
      {"kind": "defines", "name": "fetch_profiles"}
    ]
  },
  {
    "id": "csv_reader_no_pandas",
    "domain": "cli_utility",
    "prompts": [
      "Build a CSV talent-list reader. Constraint for this whole task: use only the standard library, never import pandas, and expose read_talent(path) as the public entry point.",
      "Start reader.py with read_talent(path) using csv.DictReader, stripping whitespace from every value, skipping rows with a blank email, and raising FileNotFoundError with a clear message when the path is missing.",
      "Now a separate concern: create notes.py with attach_note(profile, note) that appends a note string to a profile's '_notes' list, creating the list if absent.",
      "Add list_notes(profile) to notes.py returning the '_notes' list, or an empty list if the profile has none.",
      "Create models.py with a Candidate dataclass: name, email, company, years_experience.",
      "Add from_dict(data) and to_dict(candidate) helpers to models.py for Candidate.",
      "Create logging_setup.py with configure_logging(level='INFO') that installs a StreamHandler with a timestamp+level+message formatter and returns the root logger.",
      "Add an override to configure_logging so it reads the TALENTTRADER_LOG_LEVEL environment variable when set, taking precedence over the level argument.",
      "Create metrics.py with a Counter class: increment(key), get(key) defaulting to 0, and reset().",
      "Add a snapshot() method to Counter in metrics.py returning a plain dict copy of all current counts.",
      "Create cache.py with a memoize(fn) decorator that caches results by its positional args in a dict.",
      "Add cache_clear() to the object memoize returns, matching functools.lru_cache's convention.",
      "Create report.py with summarize_profiles(profiles) returning a dict of company -> count.",
      "Change summarize_profiles in report.py to return a list of (company, count) tuples sorted by count descending.",
      "Create storage.py with save_profiles_json(profiles, path) using json.dump with indent=2, creating parent directories as needed.",
      "Add load_profiles_json(path) to storage.py as the inverse of save_profiles_json, returning [] if the file does not exist.",
      "Create cli.py with a function read_url_list(path) that reads one URL per line, skipping blank lines and lines starting with '#'.",
      "Add build_parser() to cli.py with argparse accepting --urls (path) and --output (path, default 'profiles.json').",
      "In notes.py, make attach_note return the updated notes list instead of None, so callers can chain it.",
      "In models.py, add a __post_init__ validation to Candidate that raises ValueError if years_experience is negative.",
      "In metrics.py, add merge(other) to Counter so a worker can combine counts from another Counter snapshot.",
      "Back in reader.py, add read_talent_files(paths) that calls read_talent for each path and concatenates the returned records in path order.",
      "Add emails_from_file(path) to reader.py that calls read_talent(path) and returns the non-empty email values in record order.",
      "Finalize reader.py."
    ],
    "expected_files": ["reader.py"],
    "requirements": [
      {"id": "no_pandas", "must_exclude": ["import pandas", "pandas"], "is_constraint": true},
      {"id": "entry_point", "must_include": ["read_talent"], "is_constraint": true},
      {"id": "dictreader", "must_include": ["DictReader"]}
    ],
    "ast_checks": [
      {"kind": "defines", "name": "read_talent"},
      {"kind": "imports", "name": "csv"}
    ]
  },
  {
    "id": "cli_no_approval_messages",
    "domain": "cli_utility",
    "prompts": [
      "Build a small talent-CSV summarizer. Constraint for this whole task: it must run fully non-interactively, never call input(), and never emit approval or confirmation language such as 'Are you sure', 'Continue?', or any text containing 'approve'.",
      "Start summarize.py with main(argv=None) using argparse for --input and --top, reading the CSV, and printing the top companies by headcount.",
      "Now a separate concern: create notes.py with attach_note(profile, note) that appends a note string to a profile's '_notes' list, creating the list if absent.",
      "Add list_notes(profile) to notes.py returning the '_notes' list, or an empty list if the profile has none.",
      "Create models.py with a Candidate dataclass: name, email, company, years_experience.",
      "Add from_dict(data) and to_dict(candidate) helpers to models.py for Candidate.",
      "Create logging_setup.py with configure_logging(level='INFO') that installs a StreamHandler with a timestamp+level+message formatter and returns the root logger.",
      "Add an override to configure_logging so it reads the TALENTTRADER_LOG_LEVEL environment variable when set, taking precedence over the level argument.",
      "Create metrics.py with a Counter class: increment(key), get(key) defaulting to 0, and reset().",
      "Add a snapshot() method to Counter in metrics.py returning a plain dict copy of all current counts.",
      "Create cache.py with a memoize(fn) decorator that caches results by its positional args in a dict.",
      "Add cache_clear() to the object memoize returns, matching functools.lru_cache's convention.",
      "Create report.py with summarize_profiles(profiles) returning a dict of company -> count.",
      "Change summarize_profiles in report.py to return a list of (company, count) tuples sorted by count descending.",
      "Create storage.py with save_profiles_json(profiles, path) using json.dump with indent=2, creating parent directories as needed.",
      "Add load_profiles_json(path) to storage.py as the inverse of save_profiles_json, returning [] if the file does not exist.",
      "Create cli.py with a function read_url_list(path) that reads one URL per line, skipping blank lines and lines starting with '#'.",
      "Add build_parser() to cli.py with argparse accepting --urls (path) and --output (path, default 'profiles.json').",
      "In notes.py, make attach_note return the updated notes list instead of None, so callers can chain it.",
      "In models.py, add a __post_init__ validation to Candidate that raises ValueError if years_experience is negative.",
      "In metrics.py, add merge(other) to Counter so a worker can combine counts from another Counter snapshot.",
      "Back in summarize.py, extract company_counts(rows) and top_companies(rows, limit), then make main() call those helpers before writing its output.",
      "Add the module execution guard that invokes main(), and make main return zero after a successful summary.",
      "Finalize summarize.py."
    ],
    "expected_files": ["summarize.py"],
    "requirements": [
      {"id": "non_interactive_no_input", "must_exclude": ["input("], "is_constraint": true},
      {"id": "no_approval_text", "must_exclude": ["Are you sure", "Continue?", "approve"], "is_constraint": true},
      {"id": "argparse", "must_include": ["argparse"]},
      {"id": "main_guard", "must_include": ["__main__"]}
    ],
    "ast_checks": [
      {"kind": "defines", "name": "main"},
      {"kind": "imports", "name": "argparse"}
    ]
  },
  {
    "id": "scraper_retry_bounded",
    "domain": "scraper",
    "prompts": [
      "Build a retrying job-board scraper. Constraints for this whole task: the retry limit is exactly 3 attempts, represented by MAX_ATTEMPTS, and every exception handler must name an exception type; expose fetch_with_retry(url) as the retry entry point.",
      "Start retry_scraper.py with MAX_ATTEMPTS = 3 and fetch_with_retry(url), using requests for HTTP, response.json() on success, time.sleep(1) between failed attempts, and re-raising the last requests.RequestException after the final attempt.",
      "Now a separate concern: create notes.py with attach_note(profile, note) that appends a note string to a profile's '_notes' list, creating the list if absent.",
      "Add list_notes(profile) to notes.py returning the '_notes' list, or an empty list if the profile has none.",
      "Create models.py with a Candidate dataclass: name, email, company, years_experience.",
      "Add from_dict(data) and to_dict(candidate) helpers to models.py for Candidate.",
      "Create logging_setup.py with configure_logging(level='INFO') that installs a StreamHandler with a timestamp+level+message formatter and returns the root logger.",
      "Add an override to configure_logging so it reads the TALENTTRADER_LOG_LEVEL environment variable when set, taking precedence over the level argument.",
      "Create metrics.py with a Counter class: increment(key), get(key) defaulting to 0, and reset().",
      "Add a snapshot() method to Counter in metrics.py returning a plain dict copy of all current counts.",
      "Create cache.py with a memoize(fn) decorator that caches results by its positional args in a dict.",
      "Add cache_clear() to the object memoize returns, matching functools.lru_cache's convention.",
      "Create report.py with summarize_profiles(profiles) returning a dict of company -> count.",
      "Change summarize_profiles in report.py to return a list of (company, count) tuples sorted by count descending.",
      "Create storage.py with save_profiles_json(profiles, path) using json.dump with indent=2, creating parent directories as needed.",
      "Add load_profiles_json(path) to storage.py as the inverse of save_profiles_json, returning [] if the file does not exist.",
      "Create cli.py with a function read_url_list(path) that reads one URL per line, skipping blank lines and lines starting with '#'.",
      "Add build_parser() to cli.py with argparse accepting --urls (path) and --output (path, default 'profiles.json').",
      "In notes.py, make attach_note return the updated notes list instead of None, so callers can chain it.",
      "In models.py, add a __post_init__ validation to Candidate that raises ValueError if years_experience is negative.",
      "In metrics.py, add merge(other) to Counter so a worker can combine counts from another Counter snapshot.",
      "Back in retry_scraper.py, add scrape_jobs(urls) that calls fetch_with_retry for every URL and returns one flat list of all jobs.",
      "Add scrape_job_board(url) as a singular wrapper around scrape_jobs([url]), returning that one board's jobs.",
      "Finalize retry_scraper.py with side-effect-free imports."
    ],
    "expected_files": ["retry_scraper.py"],
    "requirements": [
      {"id": "bounded_attempts", "must_include": ["MAX_ATTEMPTS", "3"], "is_constraint": true},
      {"id": "no_bare_except_text", "must_exclude": ["except:"], "is_constraint": true},
      {"id": "retry_entry", "must_include": ["fetch_with_retry"], "is_constraint": true},
      {"id": "delay", "must_include": ["time.sleep"]}
    ],
    "ast_checks": [
      {"kind": "defines", "name": "fetch_with_retry"},
      {"kind": "defines", "name": "scrape_jobs"},
      {"kind": "imports", "name": "requests"},
      {"kind": "forbid_bare_except", "name": "unused"}
    ]
  },
  {
    "id": "jsonl_profile_allowlist",
    "domain": "talenttrader",
    "prompts": [
      "Create a JSONL profile exporter. Constraints for this whole task: serialize only the allowlisted fields name, email, company, and title, define them in ALLOWED_FIELDS, and never use pickle.",
      "Start export_profiles.py with ALLOWED_FIELDS and safe_profile(profile), returning a new dictionary projected from those field names.",
      "Now a separate concern: create notes.py with attach_note(profile, note) that appends a note string to a profile's '_notes' list, creating the list if absent.",
      "Add list_notes(profile) to notes.py returning the '_notes' list, or an empty list if the profile has none.",
      "Create models.py with a Candidate dataclass: name, email, company, years_experience.",
      "Add from_dict(data) and to_dict(candidate) helpers to models.py for Candidate.",
      "Create logging_setup.py with configure_logging(level='INFO') that installs a StreamHandler with a timestamp+level+message formatter and returns the root logger.",
      "Add an override to configure_logging so it reads the TALENTTRADER_LOG_LEVEL environment variable when set, taking precedence over the level argument.",
      "Create metrics.py with a Counter class: increment(key), get(key) defaulting to 0, and reset().",
      "Add a snapshot() method to Counter in metrics.py returning a plain dict copy of all current counts.",
      "Create cache.py with a memoize(fn) decorator that caches results by its positional args in a dict.",
      "Add cache_clear() to the object memoize returns, matching functools.lru_cache's convention.",
      "Create report.py with summarize_profiles(profiles) returning a dict of company -> count.",
      "Change summarize_profiles in report.py to return a list of (company, count) tuples sorted by count descending.",
      "Create storage.py with save_profiles_json(profiles, path) using json.dump with indent=2, creating parent directories as needed.",
      "Add load_profiles_json(path) to storage.py as the inverse of save_profiles_json, returning [] if the file does not exist.",
      "Create cli.py with a function read_url_list(path) that reads one URL per line, skipping blank lines and lines starting with '#'.",
      "Add build_parser() to cli.py with argparse accepting --urls (path) and --output (path, default 'profiles.json').",
      "In notes.py, make attach_note return the updated notes list instead of None, so callers can chain it.",
      "In models.py, add a __post_init__ validation to Candidate that raises ValueError if years_experience is negative.",
      "In metrics.py, add merge(other) to Counter so a worker can combine counts from another Counter snapshot.",
      "Back in export_profiles.py, add export_profiles(profiles, path) that creates parent directories, writes one safe_profile row per line with json.dumps and ensure_ascii=False using UTF-8, and returns the number written.",
      "Add export_profile(profile, path) as a singular wrapper that delegates to export_profiles([profile], path).",
      "Finalize export_profiles.py without import-time output."
    ],
    "expected_files": ["export_profiles.py"],
    "requirements": [
      {"id": "field_allowlist", "must_include": ["ALLOWED_FIELDS", "name", "email", "company", "title"], "is_constraint": true},
      {"id": "no_pickle", "must_exclude": ["pickle"], "is_constraint": true},
      {"id": "unicode_jsonl", "must_include": ["json.dumps", "ensure_ascii=False"]},
      {"id": "safe_projection", "must_include": ["safe_profile"]}
    ],
    "ast_checks": [
      {"kind": "defines", "name": "safe_profile"},
      {"kind": "defines", "name": "export_profiles"},
      {"kind": "imports", "name": "json"}
    ]
  },
  {
    "id": "robots_aware_crawler",
    "domain": "scraper",
    "prompts": [
      "Implement a robots-aware career-page crawler. Constraints for this whole task: use only the standard library, never import requests, and check RobotFileParser.can_fetch before retrieving a page.",
      "Start robots_crawler.py with can_fetch(url, user_agent), deriving the site's /robots.txt URL and using urllib.robotparser.RobotFileParser to read it and return its can_fetch decision.",
      "Now a separate concern: create notes.py with attach_note(profile, note) that appends a note string to a profile's '_notes' list, creating the list if absent.",
      "Add list_notes(profile) to notes.py returning the '_notes' list, or an empty list if the profile has none.",
      "Create models.py with a Candidate dataclass: name, email, company, years_experience.",
      "Add from_dict(data) and to_dict(candidate) helpers to models.py for Candidate.",
      "Create logging_setup.py with configure_logging(level='INFO') that installs a StreamHandler with a timestamp+level+message formatter and returns the root logger.",
      "Add an override to configure_logging so it reads the TALENTTRADER_LOG_LEVEL environment variable when set, taking precedence over the level argument.",
      "Create metrics.py with a Counter class: increment(key), get(key) defaulting to 0, and reset().",
      "Add a snapshot() method to Counter in metrics.py returning a plain dict copy of all current counts.",
      "Create cache.py with a memoize(fn) decorator that caches results by its positional args in a dict.",
      "Add cache_clear() to the object memoize returns, matching functools.lru_cache's convention.",
      "Create report.py with summarize_profiles(profiles) returning a dict of company -> count.",
      "Change summarize_profiles in report.py to return a list of (company, count) tuples sorted by count descending.",
      "Create storage.py with save_profiles_json(profiles, path) using json.dump with indent=2, creating parent directories as needed.",
      "Add load_profiles_json(path) to storage.py as the inverse of save_profiles_json, returning [] if the file does not exist.",
      "Create cli.py with a function read_url_list(path) that reads one URL per line, skipping blank lines and lines starting with '#'.",
      "Add build_parser() to cli.py with argparse accepting --urls (path) and --output (path, default 'profiles.json').",
      "In notes.py, make attach_note return the updated notes list instead of None, so callers can chain it.",
      "In models.py, add a __post_init__ validation to Candidate that raises ValueError if years_experience is negative.",
      "In metrics.py, add merge(other) to Counter so a worker can combine counts from another Counter snapshot.",
      "Back in robots_crawler.py, add fetch_page(url, user_agent) that raises PermissionError when can_fetch denies the URL; otherwise build an urllib.request.Request with the supplied User-Agent, open it, and return decoded text.",
      "Add crawl_allowed(urls, user_agent) to robots_crawler.py, returning a mapping from each permitted URL to the text obtained through fetch_page while omitting denied URLs.",
      "Finalize robots_crawler.py with no network access at import time."
    ],
    "expected_files": ["robots_crawler.py"],
    "requirements": [
      {"id": "stdlib_only", "must_exclude": ["requests"], "is_constraint": true},
      {"id": "robots_gate", "must_include": ["RobotFileParser", "can_fetch"], "is_constraint": true},
      {"id": "user_agent", "must_include": ["User-Agent"]},
      {"id": "denied_error", "must_include": ["PermissionError"]}
    ],
    "ast_checks": [
      {"kind": "defines", "name": "can_fetch"},
      {"kind": "defines", "name": "fetch_page"},
      {"kind": "defines", "name": "crawl_allowed"},
      {"kind": "imports", "name": "urllib"}
    ]
  },
  {
    "id": "archive_cli_dry_run",
    "domain": "cli_utility",
    "prompts": [
      "Build a candidate-archive CLI. Constraints for this whole task: archive_candidates must default to dry_run=True, the only mutation opt-in is an explicit --apply store_true flag, and the program must never call input().",
      "Start archive_candidates.py with archive_candidates(database, before, dry_run=True), counting matching candidates and performing the archive update plus commit only when dry_run is false, then returning the count.",
      "Now a separate concern: create notes.py with attach_note(profile, note) that appends a note string to a profile's '_notes' list, creating the list if absent.",
      "Add list_notes(profile) to notes.py returning the '_notes' list, or an empty list if the profile has none.",
      "Create models.py with a Candidate dataclass: name, email, company, years_experience.",
      "Add from_dict(data) and to_dict(candidate) helpers to models.py for Candidate.",
      "Create logging_setup.py with configure_logging(level='INFO') that installs a StreamHandler with a timestamp+level+message formatter and returns the root logger.",
      "Add an override to configure_logging so it reads the TALENTTRADER_LOG_LEVEL environment variable when set, taking precedence over the level argument.",
      "Create metrics.py with a Counter class: increment(key), get(key) defaulting to 0, and reset().",
      "Add a snapshot() method to Counter in metrics.py returning a plain dict copy of all current counts.",
      "Create cache.py with a memoize(fn) decorator that caches results by its positional args in a dict.",
      "Add cache_clear() to the object memoize returns, matching functools.lru_cache's convention.",
      "Create report.py with summarize_profiles(profiles) returning a dict of company -> count.",
      "Change summarize_profiles in report.py to return a list of (company, count) tuples sorted by count descending.",
      "Create storage.py with save_profiles_json(profiles, path) using json.dump with indent=2, creating parent directories as needed.",
      "Add load_profiles_json(path) to storage.py as the inverse of save_profiles_json, returning [] if the file does not exist.",
      "Create cli.py with a function read_url_list(path) that reads one URL per line, skipping blank lines and lines starting with '#'.",
      "Add build_parser() to cli.py with argparse accepting --urls (path) and --output (path, default 'profiles.json').",
      "In notes.py, make attach_note return the updated notes list instead of None, so callers can chain it.",
      "In models.py, add a __post_init__ validation to Candidate that raises ValueError if years_experience is negative.",
      "In metrics.py, add merge(other) to Counter so a worker can combine counts from another Counter snapshot.",
      "Back in archive_candidates.py, add build_parser() with argparse options --database, --before, and --apply using action='store_true'.",
      "Add main(argv=None) that parses arguments, calls archive_candidates with dry_run set from the parsed apply flag, prints the returned count, and add the module's __main__ guard.",
      "Finalize archive_candidates.py."
    ],
    "expected_files": ["archive_candidates.py"],
    "requirements": [
      {"id": "dry_run_default", "must_include": ["dry_run=True", "--apply", "store_true"], "is_constraint": true},
      {"id": "no_interactive_prompt", "must_exclude": ["input("], "is_constraint": true},
      {"id": "argparse_cli", "must_include": ["argparse", "__main__"]}
    ],
    "ast_checks": [
      {"kind": "defines", "name": "build_parser"},
      {"kind": "defines", "name": "archive_candidates"},
      {"kind": "defines", "name": "main"},
      {"kind": "imports", "name": "argparse"}
    ]
  },
  {
    "id": "sqlite_parameterized_store",
    "domain": "talenttrader",
    "prompts": [
      "Create a SQLite candidate store. Constraint for this whole task: every SQL value must use ? placeholder parameters, and SQL must never be constructed with f-strings, concatenation, or .format().",
      "Start candidate_store.py with connect(path), configuring sqlite3.Row as the row factory, and create_schema(connection) using fixed DDL for name, email, and company columns.",
      "Now a separate concern: create notes.py with attach_note(profile, note) that appends a note string to a profile's '_notes' list, creating the list if absent.",
      "Add list_notes(profile) to notes.py returning the '_notes' list, or an empty list if the profile has none.",
      "Create models.py with a Candidate dataclass: name, email, company, years_experience.",
      "Add from_dict(data) and to_dict(candidate) helpers to models.py for Candidate.",
      "Create logging_setup.py with configure_logging(level='INFO') that installs a StreamHandler with a timestamp+level+message formatter and returns the root logger.",
      "Add an override to configure_logging so it reads the TALENTTRADER_LOG_LEVEL environment variable when set, taking precedence over the level argument.",
      "Create metrics.py with a Counter class: increment(key), get(key) defaulting to 0, and reset().",
      "Add a snapshot() method to Counter in metrics.py returning a plain dict copy of all current counts.",
      "Create cache.py with a memoize(fn) decorator that caches results by its positional args in a dict.",
      "Add cache_clear() to the object memoize returns, matching functools.lru_cache's convention.",
      "Create report.py with summarize_profiles(profiles) returning a dict of company -> count.",
      "Change summarize_profiles in report.py to return a list of (company, count) tuples sorted by count descending.",
      "Create storage.py with save_profiles_json(profiles, path) using json.dump with indent=2, creating parent directories as needed.",
      "Add load_profiles_json(path) to storage.py as the inverse of save_profiles_json, returning [] if the file does not exist.",
      "Create cli.py with a function read_url_list(path) that reads one URL per line, skipping blank lines and lines starting with '#'.",
      "Add build_parser() to cli.py with argparse accepting --urls (path) and --output (path, default 'profiles.json').",
      "In notes.py, make attach_note return the updated notes list instead of None, so callers can chain it.",
      "In models.py, add a __post_init__ validation to Candidate that raises ValueError if years_experience is negative.",
      "In metrics.py, add merge(other) to Counter so a worker can combine counts from another Counter snapshot.",
      "Back in candidate_store.py, add save_candidate(connection, candidate) to insert the candidate's name, email, and company and commit the transaction.",
      "Add find_candidates(connection, company) to select those three columns for the company and return a list of plain dictionaries converted from the sqlite3.Row results.",
      "Finalize candidate_store.py."
    ],
    "expected_files": ["candidate_store.py"],
    "requirements": [
      {"id": "placeholder_parameters", "must_include": ["?"], "must_exclude": ["f\"SELECT", "f'SELECT", "f\"INSERT", "f'INSERT", ".format("], "is_constraint": true},
      {"id": "sqlite_backend", "must_include": ["sqlite3", "sqlite3.Row"]},
      {"id": "transaction_commit", "must_include": ["commit("]}
    ],
    "ast_checks": [
      {"kind": "defines", "name": "connect"},
      {"kind": "defines", "name": "create_schema"},
      {"kind": "defines", "name": "save_candidate"},
      {"kind": "defines", "name": "find_candidates"},
      {"kind": "imports", "name": "sqlite3"}
    ]
  },
  {
    "id": "env_token_config",
    "domain": "cli_utility",
    "prompts": [
      "Implement TalentTrader API configuration. Constraint for this whole task: the API token must come only from os.getenv('TALENTTRADER_API_TOKEN'), with no hard-coded value or fallback token, and a missing or blank token must raise RuntimeError.",
      "Start config.py with load_config(), reading TALENTTRADER_API_URL with default https://api.talenttrader.example, reading TALENTTRADER_API_TOKEN, parsing TALENTTRADER_TIMEOUT with default 30, and returning api_url, api_token, and timeout in a dictionary.",
      "Now a separate concern: create notes.py with attach_note(profile, note) that appends a note string to a profile's '_notes' list, creating the list if absent.",
      "Add list_notes(profile) to notes.py returning the '_notes' list, or an empty list if the profile has none.",
      "Create models.py with a Candidate dataclass: name, email, company, years_experience.",
      "Add from_dict(data) and to_dict(candidate) helpers to models.py for Candidate.",
      "Create logging_setup.py with configure_logging(level='INFO') that installs a StreamHandler with a timestamp+level+message formatter and returns the root logger.",
      "Add an override to configure_logging so it reads the TALENTTRADER_LOG_LEVEL environment variable when set, taking precedence over the level argument.",
      "Create metrics.py with a Counter class: increment(key), get(key) defaulting to 0, and reset().",
      "Add a snapshot() method to Counter in metrics.py returning a plain dict copy of all current counts.",
      "Create cache.py with a memoize(fn) decorator that caches results by its positional args in a dict.",
      "Add cache_clear() to the object memoize returns, matching functools.lru_cache's convention.",
      "Create report.py with summarize_profiles(profiles) returning a dict of company -> count.",
      "Change summarize_profiles in report.py to return a list of (company, count) tuples sorted by count descending.",
      "Create storage.py with save_profiles_json(profiles, path) using json.dump with indent=2, creating parent directories as needed.",
      "Add load_profiles_json(path) to storage.py as the inverse of save_profiles_json, returning [] if the file does not exist.",
      "Create cli.py with a function read_url_list(path) that reads one URL per line, skipping blank lines and lines starting with '#'.",
      "Add build_parser() to cli.py with argparse accepting --urls (path) and --output (path, default 'profiles.json').",
      "In notes.py, make attach_note return the updated notes list instead of None, so callers can chain it.",
      "In models.py, add a __post_init__ validation to Candidate that raises ValueError if years_experience is negative.",
      "In metrics.py, add merge(other) to Counter so a worker can combine counts from another Counter snapshot.",
      "Back in config.py, add validate_config(config) that raises ValueError when timeout is not positive and returns the config otherwise.",
      "Make load_config call validate_config before returning, and add a module docstring describing the environment-backed configuration loader.",
      "Finalize config.py."
    ],
    "expected_files": ["config.py"],
    "requirements": [
      {"id": "environment_token", "must_include": ["TALENTTRADER_API_TOKEN", "getenv"], "is_constraint": true},
      {"id": "no_sample_secret", "must_exclude": ["secret-token", "changeme", "hardcoded-token"], "is_constraint": true},
      {"id": "missing_token_error", "must_include": ["RuntimeError"]},
      {"id": "timeout", "must_include": ["TALENTTRADER_TIMEOUT"]}
    ],
    "ast_checks": [
      {"kind": "defines", "name": "load_config"},
      {"kind": "defines", "name": "validate_config"},
      {"kind": "imports", "name": "os"}
    ]
  },
  {
    "id": "stable_candidate_dedupe",
    "domain": "talenttrader",
    "prompts": [
      "Build candidate deduplication. Constraints for this whole task: compare non-empty email addresses after strip().casefold(), keep the first-seen candidate in original input order, and treat profiles with missing or blank email as distinct.",
      "Start dedupe.py with normalize_email(value) returning the stripped, case-folded email string, and dedupe_profiles(profiles) using a seen set only for non-empty normalized email keys while appending unchanged profile dictionaries to the result.",
      "Now a separate concern: create notes.py with attach_note(profile, note) that appends a note string to a profile's '_notes' list, creating the list if absent.",
      "Add list_notes(profile) to notes.py returning the '_notes' list, or an empty list if the profile has none.",
      "Create models.py with a Candidate dataclass: name, email, company, years_experience.",
      "Add from_dict(data) and to_dict(candidate) helpers to models.py for Candidate.",
      "Create logging_setup.py with configure_logging(level='INFO') that installs a StreamHandler with a timestamp+level+message formatter and returns the root logger.",
      "Add an override to configure_logging so it reads the TALENTTRADER_LOG_LEVEL environment variable when set, taking precedence over the level argument.",
      "Create metrics.py with a Counter class: increment(key), get(key) defaulting to 0, and reset().",
      "Add a snapshot() method to Counter in metrics.py returning a plain dict copy of all current counts.",
      "Create cache.py with a memoize(fn) decorator that caches results by its positional args in a dict.",
      "Add cache_clear() to the object memoize returns, matching functools.lru_cache's convention.",
      "Create report.py with summarize_profiles(profiles) returning a dict of company -> count.",
      "Change summarize_profiles in report.py to return a list of (company, count) tuples sorted by count descending.",
      "Create storage.py with save_profiles_json(profiles, path) using json.dump with indent=2, creating parent directories as needed.",
      "Add load_profiles_json(path) to storage.py as the inverse of save_profiles_json, returning [] if the file does not exist.",
      "Create cli.py with a function read_url_list(path) that reads one URL per line, skipping blank lines and lines starting with '#'.",
      "Add build_parser() to cli.py with argparse accepting --urls (path) and --output (path, default 'profiles.json').",
      "In notes.py, make attach_note return the updated notes list instead of None, so callers can chain it.",
      "In models.py, add a __post_init__ validation to Candidate that raises ValueError if years_experience is negative.",
      "In metrics.py, add merge(other) to Counter so a worker can combine counts from another Counter snapshot.",
      "Back in dedupe.py, add dedupe_batches(batches) that concatenates profiles from each batch in arrival order and delegates the combined list to dedupe_profiles.",
      "Add duplicate_count(profiles) that compares the input length with len(dedupe_profiles(profiles)) and returns how many entries were removed.",
      "Finalize dedupe.py."
    ],
    "expected_files": ["dedupe.py"],
    "requirements": [
      {"id": "normalized_email", "must_include": ["strip", "casefold"], "is_constraint": true},
      {"id": "stable_seen", "must_include": ["seen", "append"], "must_exclude": ["sorted(", "set(profiles)", "list(set("], "is_constraint": true},
      {"id": "email_key", "must_include": ["email"]}
    ],
    "ast_checks": [
      {"kind": "defines", "name": "normalize_email"},
      {"kind": "defines", "name": "dedupe_profiles"}
    ]
  },
  {
    "id": "csv_fixed_columns",
    "domain": "cli_utility",
    "prompts": [
      "Create a candidate CSV writer. Constraints for this whole task: output columns must be exactly name, email, company in that order through a fixed FIELDNAMES constant, and never use pandas.",
      "Start csv_export.py with FIELDNAMES and write_candidates(path, candidates), using csv.DictWriter with extrasaction='ignore', writing a header, opening UTF-8 with newline='', creating parent directories, and returning the row count.",
      "Now a separate concern: create notes.py with attach_note(profile, note) that appends a note string to a profile's '_notes' list, creating the list if absent.",
      "Add list_notes(profile) to notes.py returning the '_notes' list, or an empty list if the profile has none.",
      "Create models.py with a Candidate dataclass: name, email, company, years_experience.",
      "Add from_dict(data) and to_dict(candidate) helpers to models.py for Candidate.",
      "Create logging_setup.py with configure_logging(level='INFO') that installs a StreamHandler with a timestamp+level+message formatter and returns the root logger.",
      "Add an override to configure_logging so it reads the TALENTTRADER_LOG_LEVEL environment variable when set, taking precedence over the level argument.",
      "Create metrics.py with a Counter class: increment(key), get(key) defaulting to 0, and reset().",
      "Add a snapshot() method to Counter in metrics.py returning a plain dict copy of all current counts.",
      "Create cache.py with a memoize(fn) decorator that caches results by its positional args in a dict.",
      "Add cache_clear() to the object memoize returns, matching functools.lru_cache's convention.",
      "Create report.py with summarize_profiles(profiles) returning a dict of company -> count.",
      "Change summarize_profiles in report.py to return a list of (company, count) tuples sorted by count descending.",
      "Create storage.py with save_profiles_json(profiles, path) using json.dump with indent=2, creating parent directories as needed.",
      "Add load_profiles_json(path) to storage.py as the inverse of save_profiles_json, returning [] if the file does not exist.",
      "Create cli.py with a function read_url_list(path) that reads one URL per line, skipping blank lines and lines starting with '#'.",
      "Add build_parser() to cli.py with argparse accepting --urls (path) and --output (path, default 'profiles.json').",
      "In notes.py, make attach_note return the updated notes list instead of None, so callers can chain it.",
      "In models.py, add a __post_init__ validation to Candidate that raises ValueError if years_experience is negative.",
      "In metrics.py, add merge(other) to Counter so a worker can combine counts from another Counter snapshot.",
      "Back in csv_export.py, add write_candidate(path, candidate) as a singular convenience wrapper that delegates to write_candidates(path, [candidate]).",
      "Add export_company(path, candidates, company) that filters matching candidates in input order and delegates the resulting rows to write_candidates.",
      "Finalize csv_export.py."
    ],
    "expected_files": ["csv_export.py"],
    "requirements": [
      {"id": "fixed_fieldnames", "must_include": ["FIELDNAMES", "name", "email", "company"], "is_constraint": true},
      {"id": "no_pandas", "must_exclude": ["pandas"], "is_constraint": true},
      {"id": "dict_writer", "must_include": ["DictWriter", "extrasaction", "ignore", "writeheader"]}
    ],
    "ast_checks": [
      {"kind": "defines", "name": "write_candidates"},
      {"kind": "imports", "name": "csv"}
    ]
  },
  {
    "id": "scraper_structured_logging",
    "domain": "scraper",
    "prompts": [
      "Add structured logging helpers for a scraper. Constraints for this whole task: use the logging module with the logger name talenttrader.scraper, never call print(), and emit profiles_found plus elapsed_seconds as structured fields.",
      "Start scraper_logging.py with configure_logging(level='INFO'), obtaining logging.getLogger('talenttrader.scraper'), converting the level with logging.getLevelName, installing a StreamHandler and Formatter with timestamp, level, logger name, and message, and returning the logger.",
      "Now a separate concern: create notes.py with attach_note(profile, note) that appends a note string to a profile's '_notes' list, creating the list if absent.",
      "Add list_notes(profile) to notes.py returning the '_notes' list, or an empty list if the profile has none.",
      "Create models.py with a Candidate dataclass: name, email, company, years_experience.",
      "Add from_dict(data) and to_dict(candidate) helpers to models.py for Candidate.",
      "Create logging_setup.py with configure_logging(level='INFO') that installs a StreamHandler with a timestamp+level+message formatter and returns the root logger.",
      "Add an override to configure_logging so it reads the TALENTTRADER_LOG_LEVEL environment variable when set, taking precedence over the level argument.",
      "Create metrics.py with a Counter class: increment(key), get(key) defaulting to 0, and reset().",
      "Add a snapshot() method to Counter in metrics.py returning a plain dict copy of all current counts.",
      "Create cache.py with a memoize(fn) decorator that caches results by its positional args in a dict.",
      "Add cache_clear() to the object memoize returns, matching functools.lru_cache's convention.",
      "Create report.py with summarize_profiles(profiles) returning a dict of company -> count.",
      "Change summarize_profiles in report.py to return a list of (company, count) tuples sorted by count descending.",
      "Create storage.py with save_profiles_json(profiles, path) using json.dump with indent=2, creating parent directories as needed.",
      "Add load_profiles_json(path) to storage.py as the inverse of save_profiles_json, returning [] if the file does not exist.",
      "Create cli.py with a function read_url_list(path) that reads one URL per line, skipping blank lines and lines starting with '#'.",
      "Add build_parser() to cli.py with argparse accepting --urls (path) and --output (path, default 'profiles.json').",
      "In notes.py, make attach_note return the updated notes list instead of None, so callers can chain it.",
      "In models.py, add a __post_init__ validation to Candidate that raises ValueError if years_experience is negative.",
      "In metrics.py, add merge(other) to Counter so a worker can combine counts from another Counter snapshot.",
      "Back in scraper_logging.py, add log_scrape_summary(url, profiles_found, elapsed_seconds) that sends one INFO event through the scraper logger with the URL in the message and the two measurements in extra.",
      "Add log_scrape_batch(summaries) that calls log_scrape_summary for each summary dictionary in input order.",
      "Finalize scraper_logging.py without configuring logging at import time."
    ],
    "expected_files": ["scraper_logging.py"],
    "requirements": [
      {"id": "named_logger", "must_include": ["getLogger", "talenttrader.scraper"], "is_constraint": true},
      {"id": "no_print", "must_exclude": ["print("], "is_constraint": true},
      {"id": "stream_handler", "must_include": ["StreamHandler", "Formatter"]},
      {"id": "structured_fields", "must_include": ["profiles_found", "elapsed_seconds"]}
    ],
    "ast_checks": [
      {"kind": "defines", "name": "configure_logging"},
      {"kind": "defines", "name": "log_scrape_summary"},
      {"kind": "imports", "name": "logging"}
    ]
  }
]
